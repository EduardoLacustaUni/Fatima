# Fatima

Estrutura para publicar páginas no GitHub Pages.

- Coloque os arquivos HTML da apresentação dentro de `/pages`.
- O arquivo `/index.htm` lista automaticamente os arquivos `.html` e `.htm` encontrados em `/pages`.
