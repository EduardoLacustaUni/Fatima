# Fatima

Estrutura para publicar páginas no GitHub Pages.

- Coloque os arquivos HTML da apresentação na raiz ou em subpastas (recomendado: `/pages`).
- O arquivo `/index.htm` lista automaticamente todos os arquivos `.html` e `.htm` do repositório (exceto o próprio `index.htm`/`index.html`).
